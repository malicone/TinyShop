@model TinyShop.Models.Product

@{
    ViewData[ "Title" ] = "Опис";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <h4>@Model.Name</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor( model => model.Price )
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor( model => model.Price )
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor( model => model.ProductGroup )
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor( model => model.ProductGroup.Name )
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor( model => model.Description )
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor( model => model.Description )
        </dd>
    </dl>
    @foreach ( var currentImage in Model.DescImages )
    {
        <a href="@Url.Action( "GetFileById", "Files", new { id = currentImage.Id } )">
            <img src="@Url.Action( "GetFileById", "Files", new { id = currentImage.Id } )" width="150" class="img-thumbnail img-fluid">
        </a>
    }
    <div class="mt-2 pt-3 border border-dark text-center align-middle">
        <p>Для замовленя товару використовуйте контакти: @GlobalOptions.ContactsAll</p>
    </div>
    <hr />
</div>
<div class="mt-3">
    <a asp-controller="Home" asp-action="Index">Назад до списку</a>
</div>