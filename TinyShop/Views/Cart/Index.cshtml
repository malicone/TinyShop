@using TinyShop.Infrastructure;
@using TinyShop.Models.ViewModels;

@model CartViewModel;
@{
    ViewData["Title"] = "Cart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model?.Cart?.Lines.Count == 0)
{    
    <div class="alert alert-info">
        Кошик пустий
    </div>
}
else
{
    <h2>Кошик</h2>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>                
                <th>Товар</th>
                <th class="text-right">Ціна</th>
                <th>Кількість</th>
                <th class="text-right">Сума</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var line in Model?.Cart?.Lines ?? Enumerable.Empty<OrderLine>())
            {
                <tr>                    
                    <td class="text-left">@line.TheProduct.Name</td>
                    <td class="text-right">@FormattingUtils.FormatPrice( line.PriceSnapshot )</td>
                    <td class="text-center">
                        <button type="button" class="btn btn-light" 
                            data-product-id="@line.TheProduct.Id" onclick="productMinus(this)">-</button>
                        <span id="productId-@line.TheProduct.Id">@line.Quantity</span>
                        <button type="button" class="btn btn-light" 
                            data-product-id="@line.TheProduct.Id" onclick="productPlus(this)">+</button>                    
                    </td>
                    <td class="text-right">@FormattingUtils.FormatPrice( line.Quantity * line.PriceSnapshot )</td>
                    <td class="text-center">
                        <form asp-action="Remove" method="post">
                            <input type="hidden" name="productId" value="@line.TheProduct.Id" />
                            <input type="hidden" name="returnUrl" value="@Model.ReturnUrl" />
                            <button type="submit" class="btn btn-sm btn-danger">
                                Видалити
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="2" class="text-right">Всього:</td>
                <td class="text-center">
                    @Model?.Cart?.ComputeTotalQuantity() @FormattingUtils.GetGoodSingleOrPlural( Model.Cart.ComputeTotalQuantity() )
                </td>
                <td class="text-right">
                    @FormattingUtils.FormatPrice( (Model?.Cart?.ComputeTotalValue()).Value )
                </td>
            </tr>
        </tfoot>
    </table>
}
<div class="text-center">
    <a class="btn btn-primary" href="@Model?.ReturnUrl">Продовжити покупки</a>
    <a class="btn btn-primary" asp-action="Checkout" asp-controller="Order">Оформити замовлення</a>
</div>

@section Scripts {
    <script type="text/javascript">
        function productPlus(element) {
            var productId = $(element).data("product-id");
            var quantityElement = $("#productId-" + productId);
            var quantity = parseInt(quantityElement.text());
            quantity++;
            quantityElement.text(quantity);
        }
        function productMinus(element) {
            var productId = $(element).data("product-id");
            var quantityElement = $("#productId-" + productId);
            var quantity = parseInt(quantityElement.text());
            if (quantity > 1) {
                quantity--;
                quantityElement.text(quantity);
            }
        }
    </script>
}